deps:
* git
* vim + vim-plug (https://github.com/junegunn/vim-plug)
* tmux
* rg (https://github.com/BurntSushi/ripgrep)
* asdf, node (for CoC), erlang, rebar3, etc... (see asdf docs)

install
* install deps (you know how to do it, right?)
* move home files
* move coc-settings.json to $HOME/.vim/
* move erlang_ls.config to $HOME/.config/erlang_ls/
